
<div class="row">
  <div class="col md-6 offset-3">

    <h1>
      <%= @user.username %>
    </h1>
</div>

<% @user.own_photos.each do |photo| %>
  <%= render "photos/photo", photo: photo %>
</div>
<%end%>
